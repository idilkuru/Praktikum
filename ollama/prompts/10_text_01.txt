You will receive a sentence. It may contain code-switching (multiple languages in one sentence).
# Nafis: first mention tokenize the sentence. also add first line and second line like my prompt. provide a list of punktuation
Your task is to label each token. do not give the masklid output only give the LID output. use chatgpt to make it more complete and clear for you by providing a good explanation for your task.
If it is a word label its language using only ISO 639-3 (eng, deu, nep, ...) codes.
If it is a {., ;} label it as "other".

Return each token and its language in this format:
<token>: <label>

Do NOT explain, summarize, or add extra lines.

Sentence:
{text}
